// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"products-service/internal/app/ent/featuresunitvalues"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// FeaturesUnitValues is the model entity for the FeaturesUnitValues schema.
type FeaturesUnitValues struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// Name holds the value of the "name" field.
	Name *string `json:"name,omitempty"`
	// Decimals holds the value of the "decimals" field.
	Decimals *int `json:"decimals,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FeaturesUnitValuesQuery when eager-loading is set.
	Edges        FeaturesUnitValuesEdges `json:"edges"`
	selectValues sql.SelectValues
}

// FeaturesUnitValuesEdges holds the relations/edges for other nodes in the graph.
type FeaturesUnitValuesEdges struct {
	// FeatureValues holds the value of the feature_values edge.
	FeatureValues []*FeaturesValues `json:"feature_values,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// FeatureValuesOrErr returns the FeatureValues value or an error if the edge
// was not loaded in eager-loading.
func (e FeaturesUnitValuesEdges) FeatureValuesOrErr() ([]*FeaturesValues, error) {
	if e.loadedTypes[0] {
		return e.FeatureValues, nil
	}
	return nil, &NotLoadedError{edge: "feature_values"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FeaturesUnitValues) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case featuresunitvalues.FieldID, featuresunitvalues.FieldDecimals:
			values[i] = new(sql.NullInt64)
		case featuresunitvalues.FieldName:
			values[i] = new(sql.NullString)
		case featuresunitvalues.FieldCreatedAt, featuresunitvalues.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FeaturesUnitValues fields.
func (fuv *FeaturesUnitValues) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case featuresunitvalues.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			fuv.ID = int(value.Int64)
		case featuresunitvalues.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				fuv.CreatedAt = value.Time
			}
		case featuresunitvalues.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				fuv.DeletedAt = new(time.Time)
				*fuv.DeletedAt = value.Time
			}
		case featuresunitvalues.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				fuv.Name = new(string)
				*fuv.Name = value.String
			}
		case featuresunitvalues.FieldDecimals:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field decimals", values[i])
			} else if value.Valid {
				fuv.Decimals = new(int)
				*fuv.Decimals = int(value.Int64)
			}
		default:
			fuv.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the FeaturesUnitValues.
// This includes values selected through modifiers, order, etc.
func (fuv *FeaturesUnitValues) Value(name string) (ent.Value, error) {
	return fuv.selectValues.Get(name)
}

// QueryFeatureValues queries the "feature_values" edge of the FeaturesUnitValues entity.
func (fuv *FeaturesUnitValues) QueryFeatureValues() *FeaturesValuesQuery {
	return NewFeaturesUnitValuesClient(fuv.config).QueryFeatureValues(fuv)
}

// Update returns a builder for updating this FeaturesUnitValues.
// Note that you need to call FeaturesUnitValues.Unwrap() before calling this method if this FeaturesUnitValues
// was returned from a transaction, and the transaction was committed or rolled back.
func (fuv *FeaturesUnitValues) Update() *FeaturesUnitValuesUpdateOne {
	return NewFeaturesUnitValuesClient(fuv.config).UpdateOne(fuv)
}

// Unwrap unwraps the FeaturesUnitValues entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (fuv *FeaturesUnitValues) Unwrap() *FeaturesUnitValues {
	_tx, ok := fuv.config.driver.(*txDriver)
	if !ok {
		panic("ent: FeaturesUnitValues is not a transactional entity")
	}
	fuv.config.driver = _tx.drv
	return fuv
}

// String implements the fmt.Stringer.
func (fuv *FeaturesUnitValues) String() string {
	var builder strings.Builder
	builder.WriteString("FeaturesUnitValues(")
	builder.WriteString(fmt.Sprintf("id=%v, ", fuv.ID))
	builder.WriteString("created_at=")
	builder.WriteString(fuv.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := fuv.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := fuv.Name; v != nil {
		builder.WriteString("name=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := fuv.Decimals; v != nil {
		builder.WriteString("decimals=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteByte(')')
	return builder.String()
}

// FeaturesUnitValuesSlice is a parsable slice of FeaturesUnitValues.
type FeaturesUnitValuesSlice []*FeaturesUnitValues
