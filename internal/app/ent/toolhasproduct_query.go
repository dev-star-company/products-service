// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"products-service/internal/app/ent/predicate"
	"products-service/internal/app/ent/products"
	"products-service/internal/app/ent/toolhasproduct"
	"products-service/internal/app/ent/tools"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ToolHasProductQuery is the builder for querying ToolHasProduct entities.
type ToolHasProductQuery struct {
	config
	ctx          *QueryContext
	order        []toolhasproduct.OrderOption
	inters       []Interceptor
	predicates   []predicate.ToolHasProduct
	withProducts *ProductsQuery
	withTools    *ToolsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ToolHasProductQuery builder.
func (thpq *ToolHasProductQuery) Where(ps ...predicate.ToolHasProduct) *ToolHasProductQuery {
	thpq.predicates = append(thpq.predicates, ps...)
	return thpq
}

// Limit the number of records to be returned by this query.
func (thpq *ToolHasProductQuery) Limit(limit int) *ToolHasProductQuery {
	thpq.ctx.Limit = &limit
	return thpq
}

// Offset to start from.
func (thpq *ToolHasProductQuery) Offset(offset int) *ToolHasProductQuery {
	thpq.ctx.Offset = &offset
	return thpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (thpq *ToolHasProductQuery) Unique(unique bool) *ToolHasProductQuery {
	thpq.ctx.Unique = &unique
	return thpq
}

// Order specifies how the records should be ordered.
func (thpq *ToolHasProductQuery) Order(o ...toolhasproduct.OrderOption) *ToolHasProductQuery {
	thpq.order = append(thpq.order, o...)
	return thpq
}

// QueryProducts chains the current query on the "products" edge.
func (thpq *ToolHasProductQuery) QueryProducts() *ProductsQuery {
	query := (&ProductsClient{config: thpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := thpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := thpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(toolhasproduct.Table, toolhasproduct.FieldID, selector),
			sqlgraph.To(products.Table, products.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, toolhasproduct.ProductsTable, toolhasproduct.ProductsColumn),
		)
		fromU = sqlgraph.SetNeighbors(thpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTools chains the current query on the "tools" edge.
func (thpq *ToolHasProductQuery) QueryTools() *ToolsQuery {
	query := (&ToolsClient{config: thpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := thpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := thpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(toolhasproduct.Table, toolhasproduct.FieldID, selector),
			sqlgraph.To(tools.Table, tools.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, toolhasproduct.ToolsTable, toolhasproduct.ToolsColumn),
		)
		fromU = sqlgraph.SetNeighbors(thpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ToolHasProduct entity from the query.
// Returns a *NotFoundError when no ToolHasProduct was found.
func (thpq *ToolHasProductQuery) First(ctx context.Context) (*ToolHasProduct, error) {
	nodes, err := thpq.Limit(1).All(setContextOp(ctx, thpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{toolhasproduct.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (thpq *ToolHasProductQuery) FirstX(ctx context.Context) *ToolHasProduct {
	node, err := thpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ToolHasProduct ID from the query.
// Returns a *NotFoundError when no ToolHasProduct ID was found.
func (thpq *ToolHasProductQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = thpq.Limit(1).IDs(setContextOp(ctx, thpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{toolhasproduct.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (thpq *ToolHasProductQuery) FirstIDX(ctx context.Context) int {
	id, err := thpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ToolHasProduct entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ToolHasProduct entity is found.
// Returns a *NotFoundError when no ToolHasProduct entities are found.
func (thpq *ToolHasProductQuery) Only(ctx context.Context) (*ToolHasProduct, error) {
	nodes, err := thpq.Limit(2).All(setContextOp(ctx, thpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{toolhasproduct.Label}
	default:
		return nil, &NotSingularError{toolhasproduct.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (thpq *ToolHasProductQuery) OnlyX(ctx context.Context) *ToolHasProduct {
	node, err := thpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ToolHasProduct ID in the query.
// Returns a *NotSingularError when more than one ToolHasProduct ID is found.
// Returns a *NotFoundError when no entities are found.
func (thpq *ToolHasProductQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = thpq.Limit(2).IDs(setContextOp(ctx, thpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{toolhasproduct.Label}
	default:
		err = &NotSingularError{toolhasproduct.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (thpq *ToolHasProductQuery) OnlyIDX(ctx context.Context) int {
	id, err := thpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ToolHasProducts.
func (thpq *ToolHasProductQuery) All(ctx context.Context) ([]*ToolHasProduct, error) {
	ctx = setContextOp(ctx, thpq.ctx, ent.OpQueryAll)
	if err := thpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ToolHasProduct, *ToolHasProductQuery]()
	return withInterceptors[[]*ToolHasProduct](ctx, thpq, qr, thpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (thpq *ToolHasProductQuery) AllX(ctx context.Context) []*ToolHasProduct {
	nodes, err := thpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ToolHasProduct IDs.
func (thpq *ToolHasProductQuery) IDs(ctx context.Context) (ids []int, err error) {
	if thpq.ctx.Unique == nil && thpq.path != nil {
		thpq.Unique(true)
	}
	ctx = setContextOp(ctx, thpq.ctx, ent.OpQueryIDs)
	if err = thpq.Select(toolhasproduct.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (thpq *ToolHasProductQuery) IDsX(ctx context.Context) []int {
	ids, err := thpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (thpq *ToolHasProductQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, thpq.ctx, ent.OpQueryCount)
	if err := thpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, thpq, querierCount[*ToolHasProductQuery](), thpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (thpq *ToolHasProductQuery) CountX(ctx context.Context) int {
	count, err := thpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (thpq *ToolHasProductQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, thpq.ctx, ent.OpQueryExist)
	switch _, err := thpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (thpq *ToolHasProductQuery) ExistX(ctx context.Context) bool {
	exist, err := thpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ToolHasProductQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (thpq *ToolHasProductQuery) Clone() *ToolHasProductQuery {
	if thpq == nil {
		return nil
	}
	return &ToolHasProductQuery{
		config:       thpq.config,
		ctx:          thpq.ctx.Clone(),
		order:        append([]toolhasproduct.OrderOption{}, thpq.order...),
		inters:       append([]Interceptor{}, thpq.inters...),
		predicates:   append([]predicate.ToolHasProduct{}, thpq.predicates...),
		withProducts: thpq.withProducts.Clone(),
		withTools:    thpq.withTools.Clone(),
		// clone intermediate query.
		sql:  thpq.sql.Clone(),
		path: thpq.path,
	}
}

// WithProducts tells the query-builder to eager-load the nodes that are connected to
// the "products" edge. The optional arguments are used to configure the query builder of the edge.
func (thpq *ToolHasProductQuery) WithProducts(opts ...func(*ProductsQuery)) *ToolHasProductQuery {
	query := (&ProductsClient{config: thpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	thpq.withProducts = query
	return thpq
}

// WithTools tells the query-builder to eager-load the nodes that are connected to
// the "tools" edge. The optional arguments are used to configure the query builder of the edge.
func (thpq *ToolHasProductQuery) WithTools(opts ...func(*ToolsQuery)) *ToolHasProductQuery {
	query := (&ToolsClient{config: thpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	thpq.withTools = query
	return thpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ToolHasProduct.Query().
//		GroupBy(toolhasproduct.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (thpq *ToolHasProductQuery) GroupBy(field string, fields ...string) *ToolHasProductGroupBy {
	thpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ToolHasProductGroupBy{build: thpq}
	grbuild.flds = &thpq.ctx.Fields
	grbuild.label = toolhasproduct.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ToolHasProduct.Query().
//		Select(toolhasproduct.FieldCreatedAt).
//		Scan(ctx, &v)
func (thpq *ToolHasProductQuery) Select(fields ...string) *ToolHasProductSelect {
	thpq.ctx.Fields = append(thpq.ctx.Fields, fields...)
	sbuild := &ToolHasProductSelect{ToolHasProductQuery: thpq}
	sbuild.label = toolhasproduct.Label
	sbuild.flds, sbuild.scan = &thpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ToolHasProductSelect configured with the given aggregations.
func (thpq *ToolHasProductQuery) Aggregate(fns ...AggregateFunc) *ToolHasProductSelect {
	return thpq.Select().Aggregate(fns...)
}

func (thpq *ToolHasProductQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range thpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, thpq); err != nil {
				return err
			}
		}
	}
	for _, f := range thpq.ctx.Fields {
		if !toolhasproduct.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if thpq.path != nil {
		prev, err := thpq.path(ctx)
		if err != nil {
			return err
		}
		thpq.sql = prev
	}
	return nil
}

func (thpq *ToolHasProductQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ToolHasProduct, error) {
	var (
		nodes       = []*ToolHasProduct{}
		_spec       = thpq.querySpec()
		loadedTypes = [2]bool{
			thpq.withProducts != nil,
			thpq.withTools != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ToolHasProduct).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ToolHasProduct{config: thpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, thpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := thpq.withProducts; query != nil {
		if err := thpq.loadProducts(ctx, query, nodes, nil,
			func(n *ToolHasProduct, e *Products) { n.Edges.Products = e }); err != nil {
			return nil, err
		}
	}
	if query := thpq.withTools; query != nil {
		if err := thpq.loadTools(ctx, query, nodes, nil,
			func(n *ToolHasProduct, e *Tools) { n.Edges.Tools = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (thpq *ToolHasProductQuery) loadProducts(ctx context.Context, query *ProductsQuery, nodes []*ToolHasProduct, init func(*ToolHasProduct), assign func(*ToolHasProduct, *Products)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ToolHasProduct)
	for i := range nodes {
		if nodes[i].ProductsID == nil {
			continue
		}
		fk := *nodes[i].ProductsID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(products.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "products_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (thpq *ToolHasProductQuery) loadTools(ctx context.Context, query *ToolsQuery, nodes []*ToolHasProduct, init func(*ToolHasProduct), assign func(*ToolHasProduct, *Tools)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ToolHasProduct)
	for i := range nodes {
		if nodes[i].ToolsID == nil {
			continue
		}
		fk := *nodes[i].ToolsID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tools.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tools_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (thpq *ToolHasProductQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := thpq.querySpec()
	_spec.Node.Columns = thpq.ctx.Fields
	if len(thpq.ctx.Fields) > 0 {
		_spec.Unique = thpq.ctx.Unique != nil && *thpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, thpq.driver, _spec)
}

func (thpq *ToolHasProductQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(toolhasproduct.Table, toolhasproduct.Columns, sqlgraph.NewFieldSpec(toolhasproduct.FieldID, field.TypeInt))
	_spec.From = thpq.sql
	if unique := thpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if thpq.path != nil {
		_spec.Unique = true
	}
	if fields := thpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, toolhasproduct.FieldID)
		for i := range fields {
			if fields[i] != toolhasproduct.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if thpq.withProducts != nil {
			_spec.Node.AddColumnOnce(toolhasproduct.FieldProductsID)
		}
		if thpq.withTools != nil {
			_spec.Node.AddColumnOnce(toolhasproduct.FieldToolsID)
		}
	}
	if ps := thpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := thpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := thpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := thpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (thpq *ToolHasProductQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(thpq.driver.Dialect())
	t1 := builder.Table(toolhasproduct.Table)
	columns := thpq.ctx.Fields
	if len(columns) == 0 {
		columns = toolhasproduct.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if thpq.sql != nil {
		selector = thpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if thpq.ctx.Unique != nil && *thpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range thpq.predicates {
		p(selector)
	}
	for _, p := range thpq.order {
		p(selector)
	}
	if offset := thpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := thpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ToolHasProductGroupBy is the group-by builder for ToolHasProduct entities.
type ToolHasProductGroupBy struct {
	selector
	build *ToolHasProductQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (thpgb *ToolHasProductGroupBy) Aggregate(fns ...AggregateFunc) *ToolHasProductGroupBy {
	thpgb.fns = append(thpgb.fns, fns...)
	return thpgb
}

// Scan applies the selector query and scans the result into the given value.
func (thpgb *ToolHasProductGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, thpgb.build.ctx, ent.OpQueryGroupBy)
	if err := thpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ToolHasProductQuery, *ToolHasProductGroupBy](ctx, thpgb.build, thpgb, thpgb.build.inters, v)
}

func (thpgb *ToolHasProductGroupBy) sqlScan(ctx context.Context, root *ToolHasProductQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(thpgb.fns))
	for _, fn := range thpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*thpgb.flds)+len(thpgb.fns))
		for _, f := range *thpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*thpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := thpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ToolHasProductSelect is the builder for selecting fields of ToolHasProduct entities.
type ToolHasProductSelect struct {
	*ToolHasProductQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (thps *ToolHasProductSelect) Aggregate(fns ...AggregateFunc) *ToolHasProductSelect {
	thps.fns = append(thps.fns, fns...)
	return thps
}

// Scan applies the selector query and scans the result into the given value.
func (thps *ToolHasProductSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, thps.ctx, ent.OpQuerySelect)
	if err := thps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ToolHasProductQuery, *ToolHasProductSelect](ctx, thps.ToolHasProductQuery, thps, thps.inters, v)
}

func (thps *ToolHasProductSelect) sqlScan(ctx context.Context, root *ToolHasProductQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(thps.fns))
	for _, fn := range thps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*thps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := thps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
